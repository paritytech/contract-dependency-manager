# CounterA

`CounterA` stores a single `uint256` counter.

Use `increment()` to add one, or `add(amount)` to increase the counter by an explicit value. The contract emits an event for each write and returns the updated count.
