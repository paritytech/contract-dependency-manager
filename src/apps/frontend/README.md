# @parity/cdm-frontend

Web dashboard for the CDM on-chain contract registry: browse and search published
contracts, view readmes, ABIs, versions, and dependencies (React 19 + Vite).

- `pnpm dev` (from the repo root) — start the dev server with workspace deps built
- `pnpm --filter @parity/cdm-frontend build` — production build
